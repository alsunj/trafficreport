@using System.Security.Claims
@model IEnumerable<App.Domain.Violations.Violation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ViolationType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ViolationName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Severity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ViolationType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ViolationName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Severity)
            </td>
            
        </tr>
}
    </tbody>
</table>
